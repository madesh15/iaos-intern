"""_template — reference module. Copy this folder to start a new module.

    cp -r app/modules/_template app/modules/<your_module_name>

Then rename the table prefixes and MANIFEST. The leading underscore keeps
this template from being auto-loaded.
"""
